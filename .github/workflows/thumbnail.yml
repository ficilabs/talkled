name: Use Thumbnail

on:
  push:
    branches: [ main ]

jobs:
  thumbnail:
    uses: username/repo-kamu/.github/workflows/thumbnail.yml@main
    with:
      url: "https://talkled-ficilabs.vercel.app"
