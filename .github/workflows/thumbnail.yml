name: Use Thumbnail

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  thumbnail:
    uses: ficilabs/automation-thumbnail/.github/workflows/thumbnail.yml@main
    with:
      url: "https://talkled-ficilabs.vercel.app/"
